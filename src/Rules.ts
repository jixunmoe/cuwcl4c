import { Add } from "./SiteRule";

// Rules generated
import * as a from "./site/AA.Config";
a.Rules.forEach(Add);

import * as b from "./site/dl.123564";
b.Rules.forEach(Add);

import * as c from "./site/dl.5xfile";
c.Rules.forEach(Add);

import * as d from "./site/dl.baidu";
d.Rules.forEach(Add);

import * as e from "./site/dl.howfile";
e.Rules.forEach(Add);

import * as f from "./site/dl.namipan.cc";
f.Rules.forEach(Add);

import * as g from "./site/dl.xuite";
g.Rules.forEach(Add);

import * as h from "./site/fm.douban";
h.Rules.forEach(Add);

import * as i from "./site/fm.moe";
i.Rules.forEach(Add);

import * as j from "./site/music.163";
j.Rules.forEach(Add);